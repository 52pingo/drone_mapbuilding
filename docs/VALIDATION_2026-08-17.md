# CityPark 全链路验证记录（2026-08-17）

## 验证配置

- UE4 4.27 + AirSim 1.8.1，CityPark `Showcase` 地图。
- PX4 v1.15.2 SITL、MicroXRCEAgent、ROS2 Humble。
- VFH+ 本地避障，飞行高度 `z=-15 m`。
- 航点：`181.55,-583.34;-395.53,-409.16;-159.49,25.13;0,0`。
- 视觉权重：本地 `best.pt`，SHA-256
  `2f3259e64d92d96411d24287e8e77c23957ce0ca39d9697d9ee5e261d8ad7094`。
- 视觉阈值 `0.25`、连续确认 2 帧、同类抓拍间隔 4 秒、每类最多 20 张。

## 任务闭环

- 4 个航点最近距离依次约 `0.5 / 0.6 / 0.5 / 0.3 m`。
- 飞行控制器在约 756 秒输出：
  `DISARMED -> mission done` 和 `=== MISSION DONE ===`。
- 最终位置约 `(-0.4, -0.0)`，已着陆并解除锁定。
- 轨迹日志 1523 行，其中导航状态 1440 行。
- 主要 VFH 动作：`go=1342`、`avoidL=30`、`avoidR=27`、
  `turn=17`、`backup=7`、`blocked=2`。

## 建图与视觉产物

- OctoMap 导出图包含约 136,082 个点，覆盖完整大环线。
- 深度话题为有效 `32FC1` 数据；任务结束时抓拍中位深度约 3.38 m。
- 314 个视觉帧，保存 74 张带框场景图。
- 检出类别及保存数量：`tree=20`、`fence=20`、`shrub=17`、
  `building=10`、`playground_equipment=5`、`pole=2`。
- 本路线未出现或未检出车辆、行人，不能据此推断这两类精度。

本记录证明当前版本可完成 CityPark 的“起飞—航点飞行—VFH 避障—
OctoMap 建图—语义抓拍—返航—降落—解除锁定—MISSION DONE”闭环；
它不等价于 27 个类别在所有场景下均已充分验证。
